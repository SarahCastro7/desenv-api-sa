class frutasService {
    getAllFrutas() {
        try {
            
        } catch (error) {
            
        }
    }

    getByiD (id) {
        return frutas.find(fruta => fruta.id === id);
    }
}

export default frutasService;

//tem que ajeitar isso aq